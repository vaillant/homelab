version: '3'

env:
  # Proxmox credentials from 1Password (bpg/proxmox provider format)
  PROXMOX_VE_ENDPOINT:
    sh: op read "op://Homelab/Proxmox/url" | sed 's|/api2/json||'
  PROXMOX_VE_API_TOKEN:
    sh: echo "$(op read 'op://Homelab/Proxmox/token_id')=$(op read 'op://Homelab/Proxmox/token_secret')"
  PROXMOX_VE_INSECURE: "true"
  # SSH private key for Proxmox disk operations
  PROXMOX_VE_SSH_PRIVATE_KEY:
    sh: op read "op://Homelab/Homelab SSH Key/private key"

vars:
  NODE: '{{.NODE | default "proxmox1"}}'
  STORAGE: '{{.STORAGE | default "local"}}'
  LXC_IMAGE: '{{.LXC_IMAGE | default "proxmox-lxc-base"}}'
  BUILDER: '{{.BUILDER | default "ssh://root@nix-builder"}}'
  NIXOS_VERSION: '{{.NIXOS_VERSION | default "24.11"}}'
  # SSH public key from 1Password for cloud-init
  SSH_PUBKEY:
    sh: op read "op://Homelab/Homelab SSH Key/public key"

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list-all

  #
  # Utility tasks
  #

  check-env:
    desc: Check required tools
    silent: true
    cmds:
      - echo "Checking required tools..."
      - command -v nix >/dev/null 2>&1 || (echo "ERROR nix is not installed" && exit 1)
      - echo "✓ nix is installed"
      - command -v tofu >/dev/null 2>&1 || (echo "ERROR tofu (OpenTofu) is not available. Run 'nix-shell'" && exit 1)
      - echo "✓ tofu (OpenTofu) is available"
      - echo ""
      - echo "Checking environment variables..."
      - test -n "$PROXMOX_VE_ENDPOINT" || (echo "ERROR PROXMOX_VE_ENDPOINT is not set" && exit 1)
      - echo "✓ PROXMOX_VE_ENDPOINT is set"
      - test -n "$PROXMOX_VE_API_TOKEN" || (echo "ERROR PROXMOX_VE_API_TOKEN is not set" && exit 1)
      - echo "✓ PROXMOX_VE_API_TOKEN is set"
      - echo ""
      - echo "✅ All checks passed!"

  check:
    desc: Test Proxmox API connection
    deps: [check-env]
    cmds:
      - echo "Testing Proxmox API connection..."
      - |
        curl -s -k -f \
          -H "Authorization: PVEAPIToken=$PROXMOX_VE_API_TOKEN" \
          "${PROXMOX_VE_ENDPOINT}api2/json/version" | jq -r '"✓ Connected to Proxmox VE v\(.data.version) (\(.data.release))"' \
          || (echo "✗ Failed to connect to Proxmox API" && exit 1)

  fmt:
    desc: Format Terraform files
    dir: terraform
    cmds:
      - tofu fmt -recursive

  #
  # Phase 1: Deploy builder
  #

  builder-init:
    desc: Initialize builder environment
    dir: terraform/env-1-builder
    cmds:
      - tofu init

  builder-plan:
    desc: Plan builder changes
    deps: [check]
    dir: terraform/env-1-builder
    cmds:
      - |
        tofu plan \
          -var="target_node={{.NODE}}" \
          -var="storage={{.STORAGE}}" \
          -var="nixos_version={{.NIXOS_VERSION}}" \
          -var="ssh_pubkey={{.SSH_PUBKEY}}"

  builder-apply:
    desc: Apply builder changes
    deps: [check]
    dir: terraform/env-1-builder
    cmds:
      - |
        tofu apply -auto-approve \
          -var="target_node={{.NODE}}" \
          -var="storage={{.STORAGE}}" \
          -var="nixos_version={{.NIXOS_VERSION}}" \
          -var="ssh_pubkey={{.SSH_PUBKEY}}"

  builder-output:
    desc: Show builder outputs
    dir: terraform/env-1-builder
    cmds:
      - tofu output

  #
  # Phase 2: Build NixOS image
  #

  build-lxc:
    desc: Build custom NixOS LXC image
    cmds:
      - echo "Building NixOS LXC image on {{.BUILDER}}..."
      - |
        nix build ./nix#{{.LXC_IMAGE}} --out-link nix/result \
          {{if .BUILDER}}--builders '{{.BUILDER}} x86_64-linux - 4 1 big-parallel'{{end}}
      - echo ""
      - ls nix/result/tarball/
      - 'echo "Upload with: task upload-lxc"'

  upload-lxc:
    desc: Upload LXC image to Proxmox
    deps: [check]
    cmds:
      - |
        TARBALL_COUNT=$(ls -1 nix/result/tarball/*.tar.xz 2>/dev/null | wc -l | tr -d ' ')
        if [ "$TARBALL_COUNT" -eq 0 ]; then
          echo "ERROR: No .tar.xz files found. Run 'task build-lxc' first."
          exit 1
        elif [ "$TARBALL_COUNT" -gt 1 ]; then
          echo "ERROR: Multiple tarballs found in nix/result/tarball/:"
          ls -1 nix/result/tarball/*.tar.xz
          echo "Please remove old builds and run 'task build-lxc' again."
          exit 1
        fi
      - echo "Uploading NixOS LXC image to {{.NODE}}..."
      - |
        TARBALL=$(ls nix/result/tarball/*.tar.xz)
        FILENAME=$(basename $TARBALL)
        curl -s -k -f \
          -H "Authorization: PVEAPIToken=$PROXMOX_VE_API_TOKEN" \
          -H "Content-Type: multipart/form-data" \
          -F "content=vztmpl" \
          -F "filename=@$TARBALL" \
          "${PROXMOX_VE_ENDPOINT}api2/json/nodes/{{.NODE}}/storage/{{.STORAGE}}/upload" \
          | jq -r '"✓ Uploaded: \(.data)"' \
          || (echo "✗ Failed to upload template" && exit 1)
        echo ""
        echo "Template available at: {{.STORAGE}}:vztmpl/$FILENAME"

  #
  # Phase 3: Deploy production
  #

  production-init:
    desc: Initialize production environment
    dir: terraform/env-2-production
    cmds:
      - tofu init

  production-plan:
    desc: Plan production changes
    deps: [check]
    dir: terraform/env-2-production
    cmds:
      - tofu plan -var="default_storage={{.STORAGE}}"

  production-apply:
    desc: Apply production changes
    deps: [check]
    dir: terraform/env-2-production
    cmds:
      - tofu apply -var="default_storage={{.STORAGE}}"

  production-output:
    desc: Show production outputs
    dir: terraform/env-2-production
    cmds:
      - tofu output
